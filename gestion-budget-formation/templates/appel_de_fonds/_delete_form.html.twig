<form method="post" action="{{ path('app_appel_de_fonds_delete', {'appelid': appel_de_fond.appelid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appel_de_fond.appelid) }}">
    <button class="btn">Supprimer</button>
</form>
