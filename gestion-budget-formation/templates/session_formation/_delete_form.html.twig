<form method="post" action="{{ path('app_session_formation_delete', {'sessionid': session_formation.sessionid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ session_formation.sessionid) }}">
    <button class="btn">Delete</button>
</form>
