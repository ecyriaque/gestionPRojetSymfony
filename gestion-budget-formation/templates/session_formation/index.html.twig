{% extends 'base.html.twig' %}

{% block title %}Liste des sessions de formation{% endblock %}

{% block body %}
    <div class="page-container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="mb-0">Sessions de formation</h1>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="{{ path('home') }}">Accueil</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Sessions</li>
                    </ol>
                </nav>
            </div>
            <a href="{{ path('app_session_formation_new') }}" class="btn btn-primary">
                <i class="fas fa-plus me-1"></i> Créer une session
            </a>
        </div>

        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} alert-dismissible fade show">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}
        {% endfor %}

        <div class="card shadow-sm">
            <div class="card-body">
                <table class="table table-striped" id="sessions-table">
                    <thead>
                        <tr>
                            <th>Projet</th>
                            <th>Date de début</th>
                            <th>Date de fin</th>
                            <th>Coût total</th>
                            <th class="text-center">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                    {% for session in session_formations %}
                        <tr>
                            <td>
                                <span class="fw-medium">{{ session.projet.nom }}</span>
                            </td>
                            <td>
                                <span class="badge bg-light text-dark">
                                    <i class="fas fa-calendar-check me-1"></i>
                                    {{ session.datedebut ? session.datedebut|date('d/m/Y') : 'Non définie' }}
                                </span>
                            </td>
                            <td>
                                <span class="badge bg-light text-dark">
                                    <i class="fas fa-calendar-times me-1"></i>
                                    {{ session.datefin ? session.datefin|date('d/m/Y') : 'Non définie' }}
                                </span>
                            </td>
                            <td>
                                <span class="fw-medium text-primary">{{ session.couttotal|number_format(2, ',', ' ') }} €</span>
                            </td>
                            <td class="text-center">
                                <div class="btn-group" role="group">
                                    <a href="{{ path('app_session_formation_show', {'sessionid': session.sessionid}) }}" class="btn btn-sm btn-info" data-bs-toggle="tooltip" title="Voir les détails">
                                        <i class="fas fa-eye"></i>
                                    </a>
                                    <a href="{{ path('app_session_formation_edit', {'sessionid': session.sessionid}) }}" class="btn btn-sm btn-warning" data-bs-toggle="tooltip" title="Modifier">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                </div>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="5" class="text-center py-4">
                                <div class="text-muted">
                                    <i class="fas fa-info-circle me-1"></i> Aucune session trouvée
                                </div>
                                <div class="mt-3">
                                    <a href="{{ path('app_session_formation_new') }}" class="btn btn-sm btn-primary">
                                        <i class="fas fa-plus me-1"></i> Créer une session
                                    </a>
                                </div>
                            </td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Initialiser les tooltips
            var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
            var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
                return new bootstrap.Tooltip(tooltipTriggerEl)
            });
            
            // Vérifier et corriger le problème de compte de colonnes
            const table = document.getElementById('sessions-table');
            if (table) {
                const headerCols = table.querySelectorAll('thead th').length;
                const rows = table.querySelectorAll('tbody tr');
                
                rows.forEach(row => {
                    // Ignorer les lignes vides ou lignes "aucun résultat"
                    if (row.querySelector('[colspan]')) return;
                    
                    const cols = row.querySelectorAll('td').length;
                    
                    // Si le nombre de colonnes dans cette ligne ne correspond pas à l'en-tête
                    if (cols !== headerCols) {
                        console.warn(`Ligne avec ${cols} colonnes trouvée, mais l'en-tête en a ${headerCols}. Correction en cours...`);
                        
                        // Si trop peu de colonnes, ajouter les colonnes manquantes
                        if (cols < headerCols) {
                            for (let i = cols; i < headerCols; i++) {
                                const td = document.createElement('td');
                                row.appendChild(td);
                            }
                        }
                        // Si trop de colonnes, supprimer les colonnes excédentaires
                        else if (cols > headerCols) {
                            for (let i = headerCols; i < cols; i++) {
                                const lastCell = row.querySelector('td:last-child');
                                if (lastCell) row.removeChild(lastCell);
                            }
                        }
                    }
                });
            }
            
            // Initialiser DataTables après correction
            setTimeout(() => {
                if ($.fn.DataTable) {
                    try {
                        $('#sessions-table').DataTable({
                            language: {
                                url: '//cdn.datatables.net/plug-ins/1.13.7/i18n/fr-FR.json'
                            },
                            responsive: true,
                            order: [[1, 'desc']],
                            columnDefs: [
                                { orderable: false, targets: 4 } // Désactiver le tri sur la colonne Actions
                            ]
                        });
                        console.log('DataTables initialisé avec succès');
                    } catch (error) {
                        console.error('Erreur lors de l\'initialisation de DataTables:', error);
                    }
                }
            }, 100); // Petit délai pour s'assurer que la correction est appliquée avant initialisation
        });
    </script>
{% endblock %}
