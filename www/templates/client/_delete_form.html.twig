<form method="post" action="{{ path('app_client_delete', {'clientid': client.clientid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.clientid) }}">
    <button class="btn btn-danger">Delete</button>
</form>