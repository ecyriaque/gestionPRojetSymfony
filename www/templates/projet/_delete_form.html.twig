<form method="post" action="{{ path('app_projet_delete', {'projetid': projet.projetid}) }}" onsubmit="return confirm('Are you sure you want to delete this projet?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ projet.projetid) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
